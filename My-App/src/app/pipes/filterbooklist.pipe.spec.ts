import { FilterbooklistPipe } from './filterbooklist.pipe';

describe('FilterbooklistPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterbooklistPipe();
    expect(pipe).toBeTruthy();
  });
});

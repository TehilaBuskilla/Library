import { Hebrew.BooleanPipe } from './hebrew.boolean.pipe';

describe('Hebrew.BooleanPipe', () => {
  it('create an instance', () => {
    const pipe = new Hebrew.BooleanPipe();
    expect(pipe).toBeTruthy();
  });
});
